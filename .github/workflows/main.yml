name: Shh

on:
  workflow_dispatch:

jobs:
  vps:
    runs-on: ubuntu-latest
    steps:
      - name: Start VPS Environment
        run: |
          echo "Updating packages..."
          sudo apt update && sudo apt install -y wget curl
          echo "✅ VPS is running. You have 1 hour to use it."
          sleep 3600  # 1 ghanta chaleg
